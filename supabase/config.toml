project_id = "clawback"

[auth]
enabled = true
site_url = "http://localhost:8081"
enable_signup = true
enable_anonymous_sign_ins = true

[functions.parse-financial-email]
# Browser preflight requests do not include a bearer token. The function handles
# OPTIONS first, then validates every POST session with Supabase Auth getUser().
verify_jwt = false
